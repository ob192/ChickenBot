[project]
name = "chickenbot"
version = "0.1.0"
description = "aiogram Telegram bot with a FastAPI control plane"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.4,<4",
    "python-dotenv>=1.0,<2",
    "asyncpg>=0.30,<1",
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.30,<1",
]
